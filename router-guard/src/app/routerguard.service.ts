import { Injectable } from '@angular/core';
import { CanActivate } from '@angular/router';

@Injectable({
  providedIn: 'root'
})
export class RouterguardService implements CanActivate {
canActivate(private ser() {
  
})
  constructor() { }
}
